#tuples in python data structure storing diff values
#creating a tuple

coordinates = (4,5)
# tuples are immutable can be modified or make anychanges in it
#it is index to 0
print(coordinates[0])


# turples and lists differences
# google for differences and advantage and more information


coords = [(4,5),(6,7)]
print(coords)
